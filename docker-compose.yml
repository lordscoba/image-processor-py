version: "3.9"

services:
  fastapi:
    build: .

    container_name: image-processor-api

    ports:
      - "8000:8000"
    deploy:
      resources:
        limits:
          memory: 1G

    volumes:
      - .:/app

    restart: unless-stopped
